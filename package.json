{
  "private": true,
  "scripts": {
    "dev": "concurrently -n vite,api -c cyan,green \"npm run dev --prefix frontend\" \"cd backend && .venv/Scripts/uvicorn main:app --reload --host 0.0.0.0 --port 1415\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && .venv/Scripts/uvicorn main:app --reload --host 0.0.0.0 --port 1415",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^4.95.0"
  }
}
