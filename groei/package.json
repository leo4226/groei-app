{
  "private": true,
  "scripts": {
    "dev": "concurrently -n vite,api -c cyan,green \"npm run dev --prefix frontend\" \"cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
